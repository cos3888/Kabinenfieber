rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    match /{document=**} {
      // Kabinenfieber clients never talk to Firestore directly.
      // Cloud Run uses its service identity and the server SDK.
      allow read, write: if false;
    }
  }
}
